#include"myqueue.h"

MyQueue::MyQueue(int size){
    Queue.Ptr = (char *)malloc(size);
    memset(&Queue, '0', sizeof(Queue));
}

MyQueue::~MyQueue(){
    ;;
}

int MyQueue::Append(char *buf, int size){
;; 
}
